{
  "widgetType": "json-viewer",
  "description": "JSON Viewer widget. Pretty-prints query results and objects for debugging and ops inspection.",
  "schema": {
    "type": "object",
    "properties": {
      "dataTemplate": { "type": "string", "description": "Template resolving to any value" },
      "collapsed": { "type": "boolean", "default": false },
      "maxHeight": { "type": "number", "description": "Max height in px before scrolling" }
    }
  },
  "events": ["onClick", "onRefresh", "onLoad"]
}
