const si = require('react-icons/si');
const keys = Object.keys(si);
console.log('Microsoft icons:', keys.filter(k => k.toLowerCase().includes('microsoft')));
console.log('SQL icons:', keys.filter(k => k.toLowerCase().includes('sql')));
console.log('Supabase:', keys.filter(k => k.toLowerCase().includes('supabase')));
console.log('BigQuery:', keys.filter(k => k.toLowerCase().includes('bigquery')));
console.log('Airtable:', keys.filter(k => k.toLowerCase().includes('airtable')));
console.log('S3:', keys.filter(k => k.toLowerCase().includes('s3')));
console.log('Elasticsearch:', keys.filter(k => k.toLowerCase().includes('elastic')));
console.log('Stripe:', keys.filter(k => k.toLowerCase().includes('stripe')));
console.log('Oracle:', keys.filter(k => k.toLowerCase().includes('oracle')));
console.log('Cockroach:', keys.filter(k => k.toLowerCase().includes('cockroach')));
console.log('Neo4j:', keys.filter(k => k.toLowerCase().includes('neo4j')));
console.log('Twilio:', keys.filter(k => k.toLowerCase().includes('twilio')));
console.log('SendGrid:', keys.filter(k => k.toLowerCase().includes('sendgrid')));
console.log('Slack:', keys.filter(k => k.toLowerCase().includes('slack')));
console.log('Notion:', keys.filter(k => k.toLowerCase().includes('notion')));
console.log('Jira:', keys.filter(k => k.toLowerCase().includes('jira')));
console.log('Analytics:', keys.filter(k => k.toLowerCase().includes('analytics')));
