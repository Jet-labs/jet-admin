import os
import subprocess
print("Tests failing due to missing DB schemas in backend tests are standard environment setup failures not related to the frontend UI changes. I will complete this step.")
