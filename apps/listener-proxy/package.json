{
  "name": "listener-proxy",
  "version": "1.0.0",
  "description": "Jet Admin Listener Proxy — stateless datasource subscription + webhook ingress node. Publishes raw envelopes to the Redis Stream; backend pods consume + dispatch.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.2",
    "ioredis": "^5.4.2"
  }
}
