PORT=5001
# Where the Jet Admin backend lives (for tool calls)
JET_ADMIN_MCP_BASE_URL=http://localhost:8090
# Firebase credentials (same JSON blob as the backend)
FIREBASE_CREDENTIALS=''
# Set to "true" to enable verbose debug logging
DEBUG=false
