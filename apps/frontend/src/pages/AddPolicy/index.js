import { RowAdditionForm } from "../../components/DataGridComponents/RowAdditionForm";
import { PolicyAdditionForm } from "../../components/PolicyComponents/PolicyAdditionForm";
import { LOCAL_CONSTANTS } from "../../constants";
/**
 *
 * @param {object} param0
 * @returns
 */
const AddPolicy = ({}) => {
  return <PolicyAdditionForm />;
};

export default AddPolicy;
